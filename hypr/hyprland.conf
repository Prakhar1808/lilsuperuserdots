################
### MONITORS ###
################
monitor=,preferred,auto,1
monitor=HDMI-A-1,1920x1080,auto,1
xwayland {
  force_zero_scaling = true
}

###################
### MY PROGRAMS ###
###################
$terminal = kitty
$fileManager = dolphin
$menu = wofi --show drun -I

#################
### AUTOSTART ###
#################
exec-once = swww-daemon
# exec-once = waybar
exec-once = wayle panel start
# exec-once = nm-applet &
exec-once = blueman-applet &
# exec-once = swaync
exec-once = hyprpm reload -n
exec-once = systemctl --user start hyprpolkitagent
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data
exec-once = wlr-randr --output HDMI-A-1 --mode 1920x1080@60Hz --pos 0,0

#############################
### ENVIRONMENT VARIABLES ###
#############################
env = XDG_MENU_PREFIX,arch-
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_QPA_PLATFORM,wayland
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1

env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = SAL_USE_VCLPLUGIN,Qt

###################
### PERMISSIONS ###
###################
# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 8

    border_size = 1

    col.active_border = 0xff9a9996 0xff241f31 45deg
    col.inactive_border = 0xff241f31 0deg

    resize_on_border = true

    allow_tearing = false

    layout = dwindle
}

decoration {
    rounding = 8
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 0.7

    fullscreen_opacity = 1

    dim_inactive = false

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 2
	    passes = 3

    	ignore_opacity = true

    	new_optimizations = true
	    xray = true

    	contrast = 1
	    brightness = 1.2
        vibrancy = 1

    	popups = true
    }
}

animations {
    enabled = true
    workspace_wraparound = false

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 5, default
}

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0.8 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hypr.land/Configuring/Variables/#gestures
# my hardware doesnt support this shit cuh
# it does now, will add them soon
# Adding the gesture now
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

###################
### KEYBINDINGS ###
###################
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, return, exec, $terminal
bind = $mainMod, B, exec, zen-browser
bind = $mainMod, Q, killactive,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, space, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, M, exec, hyprlock
bind = shift, PRINT, exec, hyprshot -m window
bind = , PRINT, exec, hyprshot -m region
bind = $mainMod, R, exec, ~/.config/waybar/scripts/reload.sh
bind = $mainMod, W, exec, waypaper
bind = $mainMod, N, exec, swaync-client -t -sw

# Move focus with mainMod + vim key bindings
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r

# Reseize windows using keymaps
bind = SUPER CTRL, H, resizeactive, -50 0
bind = SUPER CTRL, L, resizeactive, 50 0
bind = SUPER CTRL, K, resizeactive, 0 -50
bind = SUPER CTRL, J, resizeactive, 0 50

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

bind = $mainMod, i, exec, cliphist list | wofi --dmenu --pre-display-cmd "echo '%s' | cut -f 2" | cliphist decode | wl-copy

##############################
### WINDOWS AND WORKSPACES ###
##############################
# PulseAudio Volume Control (pavucontrol)
windowrule = float on, center on, size 600 500, match:class ^(org\.pulseaudio\.pavucontrol)$

# PulseAudio Volume Control (pavucontrol)
windowrule = float on, center on, size 800 500, match:class ^(waypaper)$

# Network Manager editor
windowrule = float on, center on, match:class ^(nm-connection-editor)$

# Kitty terminal
# windowrule = no_blur off, opaque off, match:class ^(kitty)$

windowrule = no_blur on, opaque on, match:class ^(zen)$

windowrule = no_blur on, opaque on, match:class ^(org.kde.gwenview)$

windowrule = no_blur on, opaque on, match:class ^(md.Obsidian)$

# Blueman Manager
windowrule = float on, center on, size 700 600, match:class ^(blueman-manager)$

# Oklch Color Picker
windowrule = float on, match:title ^(Oklch Color Picker)$

# kleopatra
windowrule = float on, center on, size 800 800, match:initial_title ^(Notepad — Kleopatra)$

# Fix dragging issues with XWayland blank windows
windowrule = no_focus on, float on, fullscreen 0, pin 0, match:class ^$, match:title ^$

##############################
###      LAYER RULES       ###
##############################
# Blur the waybar layer
layerrule = blur on, match:namespace ^waybar$

# Blur and ignore alpha below 0.5 for wofi
layerrule = blur on, ignore_alpha 0.5, match:namespace ^wofi$

# Dim everything behind a launcher layer
layerrule = dim_around on, match:namespace ^launcher$

# TODO: Might put these plugins config in some other file
# Nevermind, chose to skip the plugins

# plugin {
#     hyprtrails {
#         history_step = 1
#         history_points = 1
#         bezier_step = 0.03
#         points_per_step = 2
#         color = 0x88ffffff
#     }
#
#     # hyprbars {
#     #     enabled = true
#     #
#     #     bar_height = 28
#     #     bar_color = rgb(1e1e1e)
#     #
#     #     # TITLE
#     #     title = true
#     #     title_align = center
#     #     bar_text_size = 10
#     #     bar_text_font = JetBrainsMono Nerd Font Mono Bold
#     #     col.text = rgb(e6e6e6)
#     #
#     #     bar_padding = 10
#     #     bar_button_padding = 12
#     #     bar_precedence_over_border = true
#     #
#     #     # Buttons (R → L)
#     #     hyprbars-button = rgb(ff6b6b), 17, , hyprctl dispatch killactive
#     #     hyprbars-button = rgb(f2c94c), 17, , hyprctl dispatch fullscreen 2
#     #     hyprbars-button = rgb(56cc9d), 17, , hyprctl dispatch togglefloating
#     # }
# }
